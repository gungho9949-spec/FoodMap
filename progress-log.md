# FoodMap 작업 진행 로그

자리 비운 사이 자동 진행 기록. 최신 항목이 아래로 추가됨.

---

## 2026-08-17 — 자리 비운 사이 자동 진행 작업 시작 (5단계 작업 목록)

사용자가 자리를 비우면서 5개 작업(①가고싶어요 기능 ②착한가격업소 데이터 ③kakaoUrl
없는 19곳 UI 예외처리 ④unmatched-list.md 재매칭 ⑤전체 흐름 점검)을 우선순위 순으로
맡기고 중간 판단은 알아서 하도록 위임함. 매 단계 끝날 때마다 커밋+push, 이 로그에
계속 기록하는 방식으로 진행. 아래부터 순서대로 기록.

### ① "가고싶어요"(찜) 기능 — 완료

`visitedRestaurants`와 완전히 분리된 localStorage 키 `savedRestaurants`로 찜 목록
관리. 지난번 방문 기록(`visitedRestaurants`) 기능과 똑같은 구조(load/save/isSaved/
toggleSaved/onSavedChange 5개 함수로만 읽고 쓰기)를 그대로 재사용해서 나중에 로그인
붙일 때 내부 구현만 바꾸면 되게 함.

- 리스트/큐레이션 카드 썸네일 좌측 상단에 별(☆/★) 토글 — 우측 상단의 방문 체크
  배지와 위치가 겹치지 않게 분리하고, 카드 클릭(상세 열기)과 별 클릭이 섞이지
  않도록 delegated 핸들러에서 `.card-save-btn`을 먼저 검사하도록 함
- 상세 패널(전체화면/바텀시트 공통)에 "가봤어요" 버튼 옆에 "가고싶어요" 버튼 추가
- 홈 화면에 "⭐ 내가 찜한 곳 (N)" 진입점 추가 — **0개면 완전히 숨김**으로 결정함
  (지시사항에 "흐리게 표시하거나 숨김" 둘 다 허용돼 있었는데, 큐레이션 섹션의
  "0개면 숨김" 패턴과 일관되게 가려고 숨김 쪽을 선택). 클릭 시 찜한 곳만 필터링된
  리스트로 이동
- 필터 드롭다운에 "찜한 곳만/안 찜한 곳만" 옵션 추가 — "가본 곳만/안 가본 곳만"
  드롭다운과 같은 자리에 옵션을 추가하는 대신, 서로 다른 축(방문 여부 vs 찜 여부)을
  한 드롭다운에 섞으면 배타적 선택이 돼버려서(둘 다 동시에 걸 수 없음) **별도
  드롭다운으로 분리**함
- **레이아웃 주의사항 반영**: 새로 추가한 `#saved-entry`는 지난번 스크롤 버그의
  원인이었던 body 최상단 고정 영역이 아니라 처음부터 `#list-view` 안(진행률 바
  바로 아래)에 배치 — mock에서 892곳 끝까지 스크롤 정상 동작 재확인함(문제 재발 없음)

**mock 검증**: 별 클릭 시 카드 아이콘 즉시 토글, `localStorage`의
`visitedRestaurants`/`savedRestaurants`가 서로 완전히 독립적으로 기록되는 것,
진행률 바에 영향 없는 것, 상세 패널의 찜 버튼도 동기화되는 것, 진입점 클릭 시
찜한 곳만 정확히 필터링되는 것, 별 클릭이 상세 패널을 열지 않는 것(이벤트 분리
확인), 892곳 끝까지 스크롤되는 것까지 모두 확인. 커밋 `689c336`.

---

## 2026-08-17 — 큐레이션 섹션 추가 후 생긴 리스트뷰 세로 스크롤 막힘 버그 수정

**증상**: 홈 화면(리스트뷰)에서 스크롤이 아래로 안 내려감.

**원인**: `#curation-sections`(가로 스크롤 섹션 4~5개)와 `#visit-progress`를
`header`/`#filter-bar`와 같은 레벨의 고정 청크(`flex-shrink:0`)로 `body` 상단에
직접 뒀던 게 문제. `body`는 `display:flex; flex-direction:column`이고 `.main`이
`flex:1`로 나머지 공간을 차지하는 구조인데, 고정 청크들의 실제 높이 합이 화면
높이를 넘으면 `.main`이 0으로 눌리고, 그 안의 `#list-view`(`overflow-y:auto`)도
`clientHeight`가 0이 돼서 스크롤할 영역 자체가 사라짐. mock에서 실측: viewport
630px인데 헤더(56)+진행률(56)+큐레이션(1104)+필터바(95) 합이 1311px — `.main`
`getBoundingClientRect().height`가 정확히 0으로 찍힘. 리스트 카드 자체는 892개
다 정상 렌더링되고 있었고(`foodListChildCount: 50`, 페이지네이션도 정상), 순전히
레이아웃 문제였음.

**수정**: `#visit-progress`·`#curation-sections`를 body 최상단 고정 영역에서 빼서
`#list-view` 안(`#food-list` 바로 위)으로 옮김. 필터바는 모바일 지도 화면에서도
계속 접근 가능해야 해서 그대로 고정 유지하고, 나머지 둘은 리스트와 함께 자연스럽게
스크롤되는 콘텐츠로 바꿈. 부수 효과로 상세 패널 진입/복귀 시 이 둘을 수동으로
숨기고 보이던 코드(`select`/`deselect`/`goHome` 3곳)가 필요 없어져서 제거 —
`#list-view` 자체의 `display` 토글에 자동으로 같이 숨겨짐(확인: 상세 진입 시
`offsetParent === null`).

**mock 검증**: `#list-view.clientHeight`가 0 → 479px로 정상화된 것 확인, 스크롤
이벤트를 반복 발생시켜 무한스크롤로 892곳 전부(마지막 카드 `chk-038`까지) 로드·
도달되는 것, "전체 892곳을 다 봤어요" 상태 텍스트까지 확인. 커밋 `9a62119`.

---

## 2026-08-17 — "가봤어요" 방문 체크 기능 추가

상세 패널(전체화면 상세/바텀시트 둘 다 — `fillPanel(prefix, food)`가 공유하는
`headHtml`에 버튼을 넣어서 자동으로 양쪽에 다 붙음)에 방문 토글 버튼을 추가.
`localStorage`의 `visitedRestaurants` 키에 id 배열로 저장하며, 나중에 로그인을
붙이면 서버 저장으로 바꿀 걸 대비해 다른 코드는 `isVisited(id)` /
`toggleVisited(id)` 두 함수만 통해서 방문 여부를 읽고 쓰게 함(직접 localStorage나
내부 Set을 만지지 않음). 상태가 바뀔 때 갱신해야 할 화면(진행률 바/큐레이션
섹션/리스트·필터/열려 있는 상세 패널 버튼)은 `onVisitedChange()` 한 곳에 모아둠.

**주의했던 순서 문제**: `isVisited()`가 리스트 카드 렌더링(`renderFoodCard`)과
필터링(`matchesFilters`)에서도 쓰이는데, 이 둘은 스크립트 최상단 근처
(`updateCount()`가 최초 호출되는 시점)에서 이미 실행됨. 방문 저장소를 원래
계획대로 파일 아래쪽(큐레이션 섹션 코드 옆)에 뒀다면 `visitedSet`이 아직
선언되기 전(TDZ)이라 첫 로드 때 `ReferenceError`가 났을 것 — `matchesFilters()`
바로 뒤, 즉 `updateCount()`의 첫 호출보다 앞으로 옮겨서 해결.

**진행률 표시**: 헤더 바로 아래, 큐레이션 섹션 위에 "내가 가본 곳 N/892" 텍스트 +
얇은 프로그레스 바. 리스트/큐레이션 카드 썸네일 우측 상단엔 녹색 체크 배지.

**필터**: 4개 드롭다운 옆에 "전체/가본 곳만/안 가본 곳만" `<select>`를 추가하고
`filterState.visited`로 `matchesFilters()`에 반영.

**큐레이션 섹션 추가**: "당신이 아직 안 가본 인기 맛집" — 미방문 + (유튜버 2명
이상 추천 또는 평점 4.5 이상) 조건으로 평점순 8곳. 방문 기록이 하나도 없으면
`getItems()`가 빈 배열을 반환해 섹션 자체가 자동으로 숨겨짐(기존 "아이템 0개면
섹션 스킵" 로직 재사용, 별도 분기 안 만듦). "더보기"는 안 가본 곳 필터로 연결.

**mock 검증**: 토글 클릭 시 localStorage/진행률/리스트 배지/열린 상세 버튼이 즉시
갱신되는 것, 새로고침 후에도 방문 기록이 그대로 남는 것, "가본 곳만/안 가본 곳만"
필터가 정확히 걸러내는 것(892 = 가본 곳 + 안 가본 곳 합), 방문 기록 0건일 때 새
섹션이 실제로 숨겨지고 1건 이상이면 나타나는 것, 모바일 바텀시트(`select()`가
`activeFood`를 먼저 설정하는 실제 경로로 재현)에서도 버튼이 정상 동작하는 것까지
모두 확인함. 커밋 `e203654`.

---

## 2026-08-16 — 홈 화면 상단에 큐레이션 테마 섹션 추가

필터 드롭다운 4개 위, 헤더 바로 아래에 가로 스크롤 카드 섹션 4개를 추가함.
`CURATION_SECTIONS` 설정 배열(`{id, title, getSubtitle, getItems, moreFilter}`)로
관리해서 나중에 섹션을 더 추가하거나 순서를 바꿀 때 배열만 손대면 되게 구성.
카드는 기존 `renderFoodCard()`를 그대로 재사용(`food-card-mini` 클래스만 추가)해서
스타일 중복 없이 폭만 160px로 줄임.

**섹션별 실제 채워진 개수** (mock 892곳 기준):
- 이번 주 추천 5곳 — 정확히 5곳 (사진 있는 835곳 중에서 우선 선정)
- 여러 유튜버가 공통으로 인정한 곳 — 18곳 (`youtubers.length >= 2`인 전체)
- 제주 여행 가면 꼭 가볼 곳 — 8곳 (제주 지역 중 평점순 상위 8)
- 혼밥하기 좋은 노포 — 8곳 (백년가게 인증 중 평점순 상위 8)

**이번 주 추천 로직**: ISO 8601 주차(월요일 기준, `getISOWeekSeed()`)를 시드로 한
결정론적 셔플(mulberry32)로 5곳을 고름. 같은 주 안에서는 새로고침해도 동일한 5곳이
나오고(mock에서 `getWeeklyPicks(5)`를 연속 호출해 동일 결과 확인함), 다음 주 월요일이
되면 시드가 바뀌어 자연스럽게 다른 5곳으로 교체됨.

**"더보기"는 필터 축이 있는 두 섹션(제주/혼밥 노포)에만 붙임** — 이번 주 추천(랜덤)과
공통 인정(유튜버 수 ≥2)은 기존 4개 필터(유튜버/지역/음식종류/인증) 중 어떤 것과도
정확히 대응되지 않아서 제외. 제주 더보기는 지역 필터를 "제주"로, 혼밥 노포 더보기는
인증 필터를 `FILTER_CERTIFICATION_OPTS`에서 "백년가게"를 부분일치로 찾은 실제 문자열
("백년가게(중소벤처기업부 공식 인증)")로 세팅 — 인증 필터가 부분일치 매칭이라 정확한
저장 문자열을 그대로 써야 해서 하드코딩 대신 옵션 목록에서 찾도록 함. mock에서 두
버튼 모두 클릭해 필터 드롭다운 값과 `filterState`, 실제 리스트 필터링 결과(제주 46곳,
백년가게 46곳)까지 확인함.

**상세 패널/뷰 전환**: 카드 클릭 시 기존 `select()`를 그대로 호출해 상세 패널이 열리고,
상세 진입 시 큐레이션 섹션은 자동으로 숨겨졌다가(`select()`의 상세 분기) 목록으로
돌아오면(`deselect()`/`goHome()`) 다시 나타나도록 함. 모바일 지도 전체화면 오버레이는
기존처럼 z-index로 덮이는 방식이라 별도 처리 불필요.

가로 스크롤(`scroll-snap-type: x mandatory`)도 mock에서 `scrollLeft` 조작으로 스냅
동작 확인(설정값 200 → 실제 186으로 카드 경계에 맞춰 스냅됨). 커밋 `fad57ec`.

---

## 2026-08-16 — 카드에 실제 식당 사진 추가 + 카드 디자인 개선

**사진 데이터 수집**: Google Places API(New)로 892곳 전체를 100개씩 9배치로 나눠 진행.
Text Search로 장소를 찾아(기존 rating-fill과 동일한 이름 유사도 검증 로직 재사용)
`photos[0]`을 Photo Media 엔드포인트(`skipHttpRedirect=true`)로 실제 이미지 URL을
받아 `photoUrl`로 저장. **API 키가 클라이언트에 노출되지 않도록** media 엔드포인트
URL이 아니라 응답의 `photoUri`(googleusercontent.com CDN 링크, 키 불필요)만 저장함
— media 엔드포인트를 그대로 저장했다면 배포 사이트 소스에 과금되는 API 키가 그대로
노출됐을 것이라 이 부분은 특히 신경 씀.

**배치별 결과:**

| 배치 | 범위 | 확보 | 사진없음 | 매칭실패 |
|---|---|---|---|---|
| 1 | 1~100 | 92 | 6 | 2 |
| 2 | 101~200 | 95 | 4 | 1 |
| 3 | 201~300 | 92 | 3 | 5 |
| 4 | 301~400 | 94 | 5 | 1 |
| 5 | 401~500 | 93 | 2 | 5 |
| 6 | 501~600 | 96 | 3 | 1 |
| 7 | 601~700 | 98 | 1 | 1 |
| 8 | 701~800 | 91 | 4 | 5 |
| 9 | 801~892 | 84 | 4 | 4 |
| **합계** | **892건** | **835 (93.6%)** | **32** | **25** |

**진행 중 발견한 버그 1건 (배치 1 직후 발견, 즉시 수정)**: `saveData()`가 필드를
항상 문자열로 직렬화하다 보니 `photoUrl: undefined`(아직 시도 안 함)와
`photoUrl: null`(시도했지만 없음)이 저장 후엔 똑같이 `null`이 되어버려서, 배치 1
저장 직후 나머지 792건이 전부 "이미 시도했지만 실패"로 잘못 기록될 뻔했음(배치 2
실행 시 "미시도 0건"으로 나와서 발견). 데이터 필드의 null/undefined 구분에 의존하는
대신, 실제로 시도한 id 목록을 별도 파일(`data/photo-progress.json`)로 추적하도록
스크립트를 고쳐서 해결 — 배치 1에서 진짜 처리된 100개(index 0~99)를 정확히
역산해서 progress 파일에 먼저 기록한 뒤 재시도해서 정상적으로 792건이 다시
잡히는 것을 확인하고 진행함. 데이터 유실이나 중복 과금 없이 정정됨.

**카드 디자인 개선 (`index.html`):**
- 카드 레이아웃을 가로형(아이콘+텍스트 한 줄)에서 세로형(사진 4:3 상단 + 정보 하단)
  으로 전면 개편. 둥근 모서리(12px, 사진 상단만)·은은한 그림자 적용.
- 사진 없으면(`photoUrl: null`) 카테고리 아이콘으로 자동 대체.
- 사진 로딩 전 회색 스켈레톤(`.card-thumb.skeleton`, shine 애니메이션) → `img`의
  `onload`/`onerror`에서 자동 해제.
- hover 시 `translateY(-4px)` + 그림자 강조 전환 효과.
- 채널별 배지 색상(`CHANNEL_COLORS` 맵): 쯔양=빨강, 성시경의 먹을텐데=파랑,
  승우아빠=주황, 또간집(풍자)=초록, 이영자=보라, 최자로드=청록,
  백반기행=갈색, 맛있는 녀석들=핑크. 인증 배지는 금색 계열로 통일.

**광고 대비 구조**: 카드 렌더링을 `renderListItem(food, index)` → `renderFoodCard(food)`
로 분리해서, 나중에 특정 위치(예: 10번째마다)에 다른 타입 카드를 끼워넣기 쉽게
만들어 둠. 지금은 실제 광고 카드를 만들지 않고 `renderListItem` 안에
`// 향후 네이티브 광고 슬롯 삽입 지점` 주석과 예시 조건문(주석 처리)만 남겨둠.

**테스트**: 사용자에게 1차 배치(100건) 결과를 먼저 보여드리고 승인받은 뒤 나머지
진행. mock 하네시로 실제 `lh3.googleusercontent.com` 사진이 카드에 정상 로딩되는
것, 4:3 비율/둥근모서리/그림자 CSS 값, `img.onload`로 스켈레톤 클래스가 해제되는
것, 채널별 배지 인라인 스타일(성시경=`#2563eb`, 인증=`#b45309`/`#fffbeb`)이 정확히
적용되는 것, `.food-card:hover` CSS 규칙이 스타일시트에 정상 등록된 것을 확인함.

전체 892건 중 835곳(93.6%)에 실사진이 붙었고, 나머지 57곳은 카테고리 아이콘으로
자연스럽게 대체됨.

---

## 2026-08-16 — 모바일 "지도로 보기" 회색 화면 버그 수정

**원인 확인 (사용자가 제시한 가설 그대로 맞았음):**
1. `#map`은 페이지 로드 시 스크립트 최상단에서 딱 한 번만 생성됨(지도 화면
   전환할 때마다 새로 만드는 방식이 아님).
2. 모바일/태블릿(1024px 미만)에서는 `.map-wrap`이 기본 `display:none`이고
   "🗺️ 지도로 보기" FAB를 눌러야 `.mobile-visible`이 붙으면서 보이는 구조라,
   지도가 생성되는 시점(페이지 로드 직후)엔 컨테이너가 아직 숨겨져 있음.
3. mock 하네시에 컨테이너 크기를 계측하는 코드를 심어서 실제로 확인함:
   생성 시점 컨테이너 크기가 정확히 `{w:0, h:0}`으로 찍힘 — 카카오맵 SDK가
   0×0 크기로 초기화되면 타일을 제대로 못 그리는 알려진 문제와 정확히 일치.
   PC(1024px 이상)는 로드 시점부터 지도가 보이는 상태라(`{w:800, h:791}`)
   애초에 이 버그가 없었음(모바일 전용 버그였다는 것도 확인됨).

**수정**: `showMapView()`(FAB 클릭 시 실행)에서 `.mobile-visible` 클래스를
붙인 뒤 `requestAnimationFrame`으로 한 프레임 미뤄서(스타일 변경이 실제
반영된 뒤 실행되도록) `map.relayout()`을 호출하도록 추가. relayout 과정에서
지도 중심이 흐트러지는 걸 막기 위해 relayout 전에 `getCenter()`로 중심을
저장해뒀다가 relayout 직후 `setCenter()`로 복원(카카오 공식 권장 패턴).

**테스트**: mock에 `relayout`/`getCenter` 호출을 계측하는 스텁을 추가해서
확인함 — FAB 클릭 시 컨테이너 크기가 `{w:0,h:0}` → `{w:375,h:812}`로 바뀌고
`relayout()`이 정확히 1회 호출됨, `지도→목록→지도` 반복 전환해도 매번
정확히 호출됨(2회째도 정상 카운트). 로직상으로는 확실히 고쳐졌음.

**⚠️ 시각적 확인은 못 함**: mock 하네시는 실제 카카오맵 타일을 그리지
않는 스텁이라(FoodMap의 카카오 JS 키가 `foodmap-kr.vercel.app` 도메인에만
등록돼 있어 로컬에서는 애초에 실제 SDK 로딩이 안 됨), "회색 화면이 실제로
지도로 바뀌는지"는 로컬에서 시각적으로 재현/확인이 불가능했음. relayout()
호출 자체가 정확한 타이밍에 정확히 실행되는 것까지만 로직으로 검증했고,
실제 지도가 정상적으로 그려지는지는 배포된 사이트(foodmap-kr.vercel.app)나
실기기 모바일 화면 캡처로 확인이 필요함.

---

## 2026-08-16 — 홈 화면을 리스트(피드) 기본 구조로 전환

기존 "지도 기본 + 마커 클릭 시 사이드바에 상세" 구조를, 에어비앤비 스타일
"리스트 기본 + PC는 지도와 좌우 분할, 모바일/태블릿은 지도를 별도 화면으로
토글" 구조로 전면 개편.

**핵심 구조 변경:**
- `#sidebar`(고정 340px, welcome/detail 토글) → `#left-panel`로 재설계.
  내부에 `#list-view`(카드 리스트, 기본 노출)와 `#detail-view`(상세 패널,
  기존 `fillPanel('d', food)` 로직 그대로 재사용)가 있고 둘 중 하나만 보임.
- `#welcome` 화면은 리스트가 그 역할을 대신하므로 제거.
- `.map-wrap`: 모바일/태블릿(1024px 미만)에서는 기본 `display:none`이다가
  `.mobile-visible` 클래스가 붙으면 `position:fixed;inset:0`으로 전체화면
  오버레이가 됨. PC(1024px 이상)에서는 `@media`로 강제 `display:block`,
  `position:relative`로 되돌려 좌측 리스트(40%, 380~480px)와 나란히 항상 노출.
- 브레이크포인트를 1024px로 통일 — 기존 바텀시트(#sheet)는 767px 기준이었는데
  이번 요청의 "PC/모바일·태블릿" 구분과 맞추기 위해 1024px로 올림. 다만 검색창
  폭 축소 같은 자잘한 공간 절약용 조정은 원래대로 767px에 남겨둠(구조적 전환과
  화면 여유 확보는 서로 다른 관심사라 분리).

**리스트(피드) 구현:**
- 카드: 이름·카테고리 아이콘(이미지 없으면 대체, `food.imageUrl` 있으면
  `<img>`로 향후 확장 가능하게 조건부 처리)·지역·평점(⭐N.N (리뷰수))·
  추천 채널 뱃지(단일=채널명, 복수="🌟N명 추천")·인증 뱃지("🏛️", hover 시
  전체 인증명 title)
- 무한 스크롤 채택(페이지네이션 대비 클릭 없이 자연스럽게 이어보기 가능,
  892개 전체를 한 번에 그리지 않고 50개씩 배치 렌더링이라 초기 로딩도 가벼움).
  `#list-view` 스크롤이 하단 400px 이내로 들어오면 다음 50개 추가.
  카드 클릭은 리스트 컨테이너에 이벤트 위임 1개로 처리(카드마다 리스너 안 닮).
- 필터 변경 시 `applyFilters()`가 `renderListInitial()`도 함께 호출해서
  리스트를 처음부터 다시 그림(기존 클러스터러 갱신 로직과 나란히 동작).

**선택(select) 로직 통합:** 카드 클릭이든 지도 마커 클릭이든 전부 `select(food, marker)`
하나로 통일하고, 내부에서 상황별로 분기:
- PC(1024px 이상): 항상 좌측 패널이 리스트→상세로 전환 (지도는 그대로 보이며
  해당 위치로 이동 + 마커 활성화)
- 모바일/태블릿 + 지도 화면에서 마커 직접 클릭: 기존 바텀시트(`fillPanel('s', ...)`)
  그대로 유지 — 지도 보면서 빠르게 훑어보는 용도라 시트가 더 적합
- 모바일/태블릿 + 리스트에서 카드 클릭: 전체화면 상세로 전환(바텀시트가 아니라
  뒤로가기 있는 풀페이지 — 지금은 리스트가 메인 화면이라 이쪽이 더 자연스러움)

**mock 테스트로 확인한 것 (전부 통과):**
- 리스트 892개 카드, 초기 50개 렌더링 후 스크롤 시 100개로 배치 추가
- 필터(유튜버=쯔양) 적용 → 리스트 436건, 클러스터러 마커 436개로 동시 반영
- PC(1280px): 좌측 패널 480px(40%, max-width 적용) + 지도 항상 노출,
  카드 클릭 시 상세 전환+지도 이동(마커 좌표와 지도 중심 좌표 일치 확인)+
  마커 활성 이미지 적용, "← 목록으로"로 리스트 복귀
- 1024px 정확히 그 값에서 분할 레이아웃으로 전환되는 것 확인(경계값 테스트)
- 태블릿(768px)·모바일(375px): 리스트 전체폭 + FAB "🗺️ 지도로 보기" 노출 확인
- FAB 클릭 → 전체화면 지도 + "☰ 목록으로" 버튼 노출, 지도에서 마커 직접
  클릭(mock 이벤트) → 바텀시트 정상 오픈, "☰ 목록으로" → 리스트로 복귀
- 모바일에서 리스트 카드 클릭 → 전체화면 상세(바텀시트 아님) 확인
- 검색 기능(기존 `selectFromSearch` → `select()` 재사용) 정상 동작
- 로고 클릭(`goHome()`) → 리스트로 복귀 + 지도 중심/줌 초기화 확인
- 인증 있는 항목 카드에 "🏛️" 뱃지 + hover 시 전체 인증명 title 노출 확인

데이터 파일은 이번 작업에서 변경하지 않음(`index.html`만 수정).

---

## 2026-08-16 — 행정안전부 착한가격업소 데이터 추가

data.go.kr "행정안전부_착한가격업소 현황"(파일데이터, 로그인 없이 다운로드, 전체
12,645행)을 다운로드. 백년가게 때와 동일한 방식(selectFileDataDownload.do →
check-limit.json으로 캡차 필요 여부 확인 → fileDownload.do, 캡차 불필요 확인 후 진행).

**필터링**: 업종 컬럼이 한식/일식/중식/양식/기타요식업/미용업/이용업/세탁업/목욕업/
숙박업/기타비요식업으로 이미 깔끔하게 구분되어 있어서, 외식업 5종(한식·일식·중식·
양식·기타요식업, 합계 9,563건)만 남기고 미용업 등은 전부 제외. 이후 대상 지역
(전북/충남/경북/충북/강원/제주)으로 좁히니 2,694건 → 착한가격 메뉴/가격 정보가 있는
곳 우선 + 지역 내 시/군 분산을 고려해 지역당 7곳씩 총 42곳 선별.

**참고**: 이 데이터셋은 위경도 좌표가 없어서(주소만 제공) 좌표까지 전부 Kakao Local
API로 새로 확보해야 했음(백년가게 때와 같은 패턴, TourAPI 때와는 다름 — TourAPI는
자체 좌표를 줬었음).

**진행 중 확인한 것**: 지난 TourAPI 작업에서 배운 대로 병합은 여전히 kakaoUrl 완전
일치 기준으로만 판단했고, 이번엔 우연히 0건 병합이 나와서(38곳 전부 신규) 혹시
놓친 게 있는지 100m 근접 스캔을 한 번 더 돌려봄 — "팔도식당"(충북 보은군)과 기존
"신토불이약초식당"(TourAPI 출처)이 39m 거리로 걸렸지만, kakaoUrl이 서로 다르고
이름도 무관해서(법주사 관광지 근처에 흔한 것처럼 다른 가게 두 곳이 가까이 있는
경우) 병합하지 않음 — 정상 판단.

검색 실패 4곳(북경루/홍카페/꽃사슴로스/원조일품국수)은 시/도 단위, 시/군 단위
쿼리로 각각 재시도해봤지만 Kakao Local에 전혀 등록돼 있지 않아 억지로 채우지 않고
제외함(작은 지방 업소라 등록 자체가 안 된 것으로 추정).

**최종 결과: 854곳 → 892곳 (38곳 신규, 병합 0건)**

| 지역 | 추가 |
|---|---|
| 강원 | 7 |
| 충북 | 5 |
| 충남 | 7 |
| 전북 | 6 |
| 경북 | 6 |
| 제주 | 7 |
| **합계** | **38곳** |

recommendNote는 요청하신 대로 "착한가격업소로 지정된 곳, 대표메뉴 OOO원" 형태로
채움(메뉴1/가격1 기준, 3,4번째 메뉴는 사용 안 함 — "대표메뉴"라는 단수 표현에 맞춤).

**필터 UI 버그 발견 및 수정**: 인증 필터 옵션 목록이 "백년가게"/"한국관광공사"
두 가지만 하드코딩되어 있어서 "착한가격업소"가 드롭다운에 아예 안 나타나는 걸
mock 테스트로 발견함 → certification 문자열을 " · " 기준으로 쪼개 각 조각을
그대로 옵션으로 쓰도록 리팩터링해서, 앞으로 다른 인증 출처가 추가돼도 이 부분을
다시 손댈 필요 없게 고침. 재검증 결과 "착한가격업소(행정안전부) 38"이 정상 노출,
인증=착한가격업소+지역=제주 조합도 정확히 7건으로 일치 확인.

---

## 2026-08-16 — 한국관광공사 TourAPI 음식점 데이터 추가

TourAPI(한국관광공사_국문 관광정보 서비스_GW)는 파일데이터가 아니라 **오픈API**라서
로그인 후 활용신청으로 받는 서비스키가 필요했음(회원가입/로그인은 대신할 수 없어
사용자에게 발급 절차 안내 요청 → 사용자가 즉시 발급해서 `.env.txt`에 추가).

**범위**: 전북/충남/경북/충북/강원/제주 6개 지역, 지역당 25곳.

**"향토음식점"/"모범업소" 태그 관련 확인 사항**: TourAPI의 실제 분류체계(categoryCode2로
조회)에는 한식/서양식/일식/중식/이색음식점/카페전통찻집/클럽 6종만 있고, "향토음식점"이나
"모범업소"에 해당하는 별도 태그는 없었음. 대신 **한식(cat3=A05020100) + 인기순 정렬로
지역당 80곳 조회 후 프랜차이즈 키워드(스타벅스/맥도날드/BBQ/이디야 등 30여개) 제외**하는
방식으로 대체 선별함.

**진행 중 발견하고 고친 문제 2가지:**

1. **지역명이 정식 명칭이라 필터가 갈라짐**: TourAPI 주소는 "충청남도"/"경상북도"/
   "충청북도"처럼 정식 시/도명을 쓰는데, 기존 `REGION_ALIAS`엔 이 매핑이 없어서
   "충남"과는 별개의 필터 항목으로 갈라질 뻔했음 → `REGION_ALIAS`에 전국 시/도
   정식명칭 전체를 보강해서 해결(향후 다른 출처를 추가해도 안전하도록).
2. **Kakao 검색 쿼리에 괄호가 있으면 0건 반환**: "축산본점식육식당(축산회관)"으로
   검색하면 0건인데 괄호를 빼고 "축산본점식육식당"으로 검색하면 정확히 매칭됨(심지어
   지난번 백년가게 배치의 "축산본점식육식당"과 정확히 같은 곳, kakaoUrl까지 동일).
   → 스크립트가 Kakao 검색용 이름에서 괄호와 그 안 내용을 제거하도록 수정.
   전체 150곳을 좌표 100m 이내 근접성으로 한 번 더 스캔해서 이 케이스 외에
   추가 누락은 없는지 확인함(울릉도 항구 근처에 다른 가게 3곳이 몰려있는 경우는
   좌표만 가까울 뿐 실제로는 서로 다른 업체라 병합하지 않음 — 병합 기준은 여전히
   좌표 근접이 아니라 kakaoUrl 완전 일치).

**최종 결과: 660곳(백년가게 반영 전 기준 705곳) → 854곳**

| 지역 | TourAPI 신규 추가 |
|---|---|
| 전북 | 25 |
| 충남 | 25 |
| 경북 | 24 (1곳은 기존 "백년가게" 항목과 병합) |
| 충북 | 25 |
| 강원 | 25 |
| 제주 | 25 |
| **합계** | **149곳 신규 + 1곳 병합** |

병합된 1곳("축산본점식육식당", 경북 영주시)은 지난 백년가게 배치에서 이미 추가된
곳이라 새로 만들지 않고 `certification`을 "백년가게(중소벤처기업부 공식 인증) ·
한국관광공사 등록 음식점"으로 이어붙였음(두 인증을 모두 받은 곳으로 정확히 표시됨).

Kakao Local API에서 해당 상호명 자체가 검색되지 않아 kakaoUrl을 못 채운 곳이 19곳
있음(대부분 아주 작은 지방 식당이라 Kakao 장소 DB에 등록 자체가 안 되어 있는 것으로
보임) — 좌표(TourAPI 제공)와 주소는 정상이라 지도 표시·상세정보는 문제없고, 다만
"카카오맵에서 보기" 버튼만 해당 항목엔 안 뜸(기존 코드가 `if (food.kakaoUrl)`로
이미 조건부 처리하고 있어서 별도 수정 불필요).

**UI 반영:**
- 필터 바에 4번째 드롭다운 "인증 전체 / 한국관광공사(150) / 백년가게(46)" 추가.
  값은 certification 문자열에 대한 부분일치로 판단해서 복수 인증(위 병합 사례처럼)도
  양쪽 필터 옵션에서 다 걸리도록 함. 3축(유튜버/지역/음식종류)과 동일하게 AND 결합.
- 상세 패널 배지를 기존 "🏅 [인증명 전체]"(길게 표시)에서 "🏛️ 공식 인증"(짧게,
  hover 시 tooltip으로 정확한 인증명 표시)으로 변경 — 인증명 전체는 이름 아래
  파란 카드(cert-section)에 그대로 표시되니 배지는 상단에 작게 두는 요청사항 반영.

mock 테스트로 필터 조합(인증=한국관광공사→150건, +지역=전북→25건, 인증만 백년가게로
바꾸면 전북 5건, 전체 리셋→854건) 및 실제 항목 클릭 시 배지/카드 렌더링 확인 완료.

---

## 2026-08-16 — 백년가게 데이터 추가 (data.go.kr) — 좌표 확보 직전 대기 중

data.go.kr "소상공인시장진흥공단_전국 백년가게 지정리스트 현황 정보"(2025-09-03 갱신,
1,407건, 로그인 없이 다운로드 가능 확인)를 다운로드해서 업체명/주소/연락처를 확인했다.
연락처 컬럼은 전부 공란(원본 안내: "사업체 연락처는 향후 데이터 정제 후 공개할 예정").

**처리 과정:**
1. 요청 지역(전북/충남/경북/충북/인천/전남/대전/강원/부산/제주) 필터링 → 742곳
2. 백년가게 데이터셋은 식당뿐 아니라 전자상가·서점·문구점 등 업종 전체를 포함하므로,
   상호명 키워드(식당/갈비/냉면/국밥/횟집 등 vs 전자/서점/문구/안경 등)로 1차 분류 → food 321곳
3. 지역별 4~5곳씩 총 **47곳** 최종 선별 (`data/baekyeongage-candidates.json`)
4. 좌표/kakaoUrl 확보용 스크립트 작성 완료 (`data/add-baekyeongage.js`) — Kakao Local
   API로 "상호명+지역명" 검색, 기존 데이터와 50m 이내 겹치면 새 항목 대신 기존 항목에
   `certification` 필드만 병합하는 로직까지 구현·문법 검증 완료
5. `index.html`의 `fillPanel()`도 함께 보강: `youtubers`가 빈 배열인 인증 전용 항목을
   클릭하면 기존 코드가 `food.youtubers[0].name`에서 크래시 나는 걸 발견해서, 인증
   배지(`.badge-cert`)/인증 섹션(`.cert-section`)을 추가하고 mock 테스트로
   단독 인증 항목·유튜버+인증 병합 항목 둘 다 정상 렌더링 확인함

**🚫 차단됨**: Kakao Local API용 REST API 키가 없음. 프로젝트에 있는 건 지도 로딩용
JavaScript 키(`28bac2696bc8d416ce3d2f349581838e`)뿐이고, 이걸로 Local API를 직접
호출해보니 `401 Unauthorized`(KA Header 필요)가 떴다 — Kakao는 JS 키와 REST API 키를
별도 발급하기 때문에 서버 사이드 검색이 불가능. 사용자에게 안내했고, `data/.env.txt`에
`KAKAO_REST_KEY`를 추가해주면 이어서 진행하기로 함.

**커밋된 것**: `data/add-baekyeongage.js`(스크립트), `data/baekyeongage-candidates.json`
(선별된 47곳), `index.html`(인증 배지 UI). **`curated-food.js`는 아직 미변경** —
좌표 없이는 지도에 표시할 수 없어서 실제 데이터 추가는 키 확보 후 진행 예정.

### 이어서 — Kakao REST API 키 확보, 실제 데이터 추가 완료 ✅

사용자가 `data/.env.txt`에 키 추가(`85715def05ece125da02d43e2e6fa85e`), 정상 작동 확인 후 진행.

**1차 실행에서 발견한 문제 3가지 — 전부 수정함:**

1. **50m 반경 병합이 전통시장에서 오탐 발생**: 순수 haversine 거리(50m)만으로 병합
   여부를 판단했더니, 같은 시장 건물에 다닥다닥 붙은 완전히 다른 업체를 같은 곳으로
   잘못 합쳐버림 — "신포순대"(순대집)가 "맷돌손칼국수"(칼국수집)에, "육거리소문난만두"
   (만두집)가 "금강설렁탕"(설렁탕집)에 병합될 뻔했음. **스크립트를 kakaoUrl(Kakao
   장소 고유 ID) 완전 일치 기준으로 수정** — "철원막국수"(진짜 동일 장소, kakaoUrl
   일치)만 정상 병합되고 나머지 둘은 독립 항목으로 남도록 고침.
2. **주소가 지번주소로 저장됨**: `best.address_name`(지번)을 썼는데 기존 660곳은
   전부 도로명주소라 스타일이 안 맞았음 — `best.road_address_name` 우선 사용으로 수정.
3. **"상호명+지역명"만으로 검색하니 동명 지점이 엉뚱한 곳으로 매칭**: "삽다리곱창전문점
   충남" → 홍성이 아니라 예산군의 다른 지점이 검색됨, "독천식당" 관련 검색도 영암군이
   아닌 목포시 지점으로 잘못 매칭됨. 원본 후보 주소의 시/군/구와 검색 결과 주소의
   시/군/구를 비교하는 사후 검증을 추가로 돌려서 발견 → 시/군/구까지 포함한 쿼리로
   재검색해서 정정함. **"남한강횟집"(충주시 동량면)은 Kakao Local에 해당 업체 자체가
   등록돼 있지 않고 동명의 완전히 다른 지역(단양군) 업체만 검색돼서, 확신 없는 매칭을
   만들지 않고 이번 47곳에서 제외**함.

(참고: "신포순대"·"인현통닭삼계탕" 주소에 있는 "인천 제물포구"는 오류가 아니라
Kakao API가 인천 중구 신포동 일대를 반환할 때 쓰는 표기 — 기존 데이터(맷돌손칼국수 등)에도
동일하게 쓰이고 있어서 일관성 있게 그대로 둠.)

**최종 결과: 660곳 → 705곳 (45곳 신규 추가 + 1곳 병합, 1곳은 매칭 확신 없어 제외)**

| 지역 | 인증 항목 수 |
|---|---|
| 강원 | 5 |
| 경북 | 5 |
| 대전 | 4 |
| 부산 | 5 |
| 인천 | 4 |
| 전남 | 5 |
| 전북 | 5 |
| 제주 | 4 |
| 충남 | 5 |
| 충북 | 4 (남한강횟집 제외로 5→4) |
| **합계** | **46건** (신규 45 + 병합 1) |

전화번호는 정부 CSV엔 전부 공란이었지만 Kakao Local API 검색 결과에 대부분 있어서
자동으로 채워짐(46건 중 45건 확보, "중앙횟집"만 Kakao에도 번호가 없어 null).

mock 테스트로 실제 신규 항목("경복궁식당" 등) 클릭 시 이름/카테고리 배지/파란색
"백년가게" 인증 배지/소개 카드/전화번호 링크/길찾기 버튼까지 전부 정상 렌더링,
마커 705개·필터 드롭다운 지역 목록도 정상 반영 확인.

---

## 2026-08-15 (Day 1)

### 1-1. videoUrl 채널별 현황 (완료)

전체 660곳, `youtubers` 배열 기준 채널별 추천 건수 / videoUrl 채워진 건수:

| 채널 | 추천 건수 | videoUrl 채워짐 |
|---|---|---|
| 쯔양 | 436 | 0 |
| 승우아빠 | 64 | 0 |
| 식객 허영만의 백반기행 | 44 | 0 |
| 최자로드 | 40 | 0 |
| 성시경의 먹을텐데 | 38 | 0 |
| 또간집(풍자) | 28 | 0 |
| 전지적 참견 시점(이영자) | 26 | 0 |
| 맛있는 녀석들 | 2 | 0 |

전 채널 videoUrl 0건 — 처음부터 매칭 필요.

### 1-2/1-3. YouTube 영상 매칭 — 🚫 차단됨 (API 키 없음)

- 환경변수/`.env` 파일 전체를 뒤졌지만 `YOUTUBE_API_KEY`(또는 유사 키)를 찾지 못함
  (`data/.env.txt`에는 `GOOGLE_PLACES_KEY`만 존재)
- 혹시 같은 GCP 프로젝트에서 YouTube Data API v3가 같이 활성화되어 있을까 싶어
  기존 `GOOGLE_PLACES_KEY`로 `youtube/v3/search` 호출을 테스트함 → 결과:
  ```
  403 PERMISSION_DENIED / API_KEY_SERVICE_BLOCKED
  "Requests to this API youtube method ... are blocked."
  ```
  → 이 키는 YouTube Data API용으로 활성화/허용되어 있지 않음. 사용 불가.
- **YouTube Data API 키는 사용자가 Google Cloud Console에서 직접 발급해야 하는 자격 증명이라
  제가 임의로 만들거나 우회할 수 없습니다.** 자리 비우신 동안 이 작업은 진행이 불가능하여
  **1번 작업(유튜브 영상 매칭)은 전체 보류**하고, 우선순위상 다음 작업들을 먼저 진행합니다.
- 키를 발급받으시면(YouTube Data API v3 활성화 필요) `data/.env.txt`에
  `YOUTUBE_API_KEY=...` 한 줄 추가해주시면 이어서 진행하겠습니다.

**→ 순서를 조정해 2번(필터 UI) → 3번(평점 재시도) → 4번(unmatched 정리) 순으로 진행합니다.**
(1번은 키 확보 시 재개)

---

### 2. 필터 UI (유튜버/지역/음식종류) — ✅ 이미 구현되어 있었음, 검증만 진행

`index.html`을 확인해보니 요청하신 필터 UI는 이미 이전 세션에서 구현되어 커밋까지
완료된 상태였습니다 (`789e03c feat: 유튜버/지역/음식종류 3축 필터 UI 추가`).
`git status`도 깨끗해서 미커밋 변경사항이 없었습니다. 재구현 대신 스펙대로 동작하는지
로컬 mock 하네시(`_testdir/`, kakao SDK 모킹 — gitignore 처리함)로 검증했습니다.

**스펙 대조 결과 (모두 충족):**
- ✅ 필터 버튼 하나로 통합 + 선택 개수 뱃지
- ✅ PC 드롭다운(`top:100px;left:16px`) / 모바일(width<768) 바텀시트(`bottom:0`, 라운드 상단, 핸들바) — 미디어쿼리로 분기
- ✅ 유튜버/지역/음식종류 3섹션 칩 다중선택
- ✅ 지역: `address.split(' ')[0]` 기반 시/도 자동 추출 (광주 5개구 특수처리 포함, 16개 지역 추출됨)
- ✅ 음식종류: `CATEGORY_MAJOR_MAP`으로 세부 category → 대분류 7종(한식/중식/일식/양식/분식/카페·디저트/기타) 매핑, 대분류 클릭 시 세부 서브칩 펼침
- ✅ 3축 AND, 축 내부 OR (`matchesFilters()`)
- ✅ `clusterer.clear()` + `addMarkers()` 재사용 (`applyFilters()`)
- ✅ "전체 660곳 중 N곳" 카운트 표시
- ✅ 복수 유튜버 장소는 배열 중 하나라도 매칭되면 노출 (`food.youtubers.some(...)`)

**필터 조합 테스트 결과 (전체 660곳 기준):**

| 조합 | 결과 건수 |
|---|---|
| 유튜버=쯔양 | 436곳 |
| 유튜버=쯔양 OR 승우아빠 | 499곳 |
| 지역=서울 | 439곳 |
| 유튜버=쯔양 AND 지역=서울 | 244곳 |
| 음식종류(대분류)=한식 | 384곳 |
| 음식종류(세부)=냉면 | 15곳 |
| 쯔양 AND 서울 AND 한식 (3축 전부) | 141곳 |
| 필터 없음(전체) | 660곳 |

모든 조합에서 `clustererMarkers` 개수가 필터링된 건수와 정확히 일치 (마커 재계산 정상).
실제 DOM 클릭 이벤트로도 재검증: 쯔양+한식 선택 → "결과 245곳 보기" → 적용 시
헤더 카운트 "245곳", `전체 660곳 중 245곳` 텍스트, 뱃지 "2", 마커 245개로 모두 일치.
모바일 뷰(375px)에서 바텀시트 정상 렌더링(핸들바, 라운드 상단, 칩 카운트 포함) 확인.

**결론: 코드 변경 없음. 커밋할 것이 없어 검증 결과만 로그로 남김.**

---

### 3. 평점 미매칭 34곳 재시도 — ✅ 완료 (6곳 추가 매칭, 28곳 여전히 null)

`data/retry-ratings.js` 작성. 시도 순서: (1) 깨진 시/도 접두어(`전남광주통합특별시` 등)
보정한 정제 주소 + 좁은 반경 → (2) 시/군/구까지만 남긴 축약 주소 + 넓은 반경(3km) →
(3) 지점명(본점/직영점/점 등) 제거한 이름 + 축약 주소. 원본 데이터의 `address`/`name`
필드는 건드리지 않고, 검색 쿼리 구성 시에만 정제함.

**추가 매칭 6곳:**

| 식당명 | 결과 | 어떤 시도에서 성공했는지 |
|---|---|---|
| 61호명태전 | ⭐4.1 (7개) | 축약주소+넓은반경 |
| 문경전통시장 상차림2호점 | ⭐3.6 (94개) | 지점명제거+축약주소 |
| 원보 | ⭐4 (4개) | 정제주소+좁은반경 (전남광주통합특별시→전라남도 보정 효과) |
| 한강반점 | ⭐3.2 (5개) | 축약주소+넓은반경 |
| 화동문어 | ⭐5 (1개) | 축약주소+넓은반경 |
| 봉자막창 부천원종점 | ⭐4.5 (24개) | 축약주소+넓은반경 |

→ **전체 660곳 중 632곳 평점 채움 (28곳 null 유지)**, 원래 626곳에서 6곳 증가.

**28곳은 그대로 null 유지** (Google에 후보는 있지만 이름이 안 맞거나, 후보 자체가 없거나,
매칭은 됐는데 그 장소 자체에 Google 평점 데이터가 없는 경우 섞여 있음).
상세 로그는 `_retry-still-null.json`에 저장(커밋 대상 아님, 4번 작업에서 정리용으로 사용 후 삭제 예정).
특이사항: "레귬"/"오프닝"/"트리드" 같은 영문 상호는 Google에 로마자(LEGUME/OPNNG/trid)로만
등록돼 있어 한글 매칭 로직으로는 원천적으로 못 잡음 — 억지로 매칭하지 않고 null 유지가 맞는 판단.

---

### 4. unmatched-list.md 정리 — ✅ 완료

[unmatched-list.md](unmatched-list.md) 생성. 두 구획으로 정리:

1. **유튜브 영상 매칭**: 1번 작업 자체가 API 키 부재로 시작 못 해서 아직 집계할 게 없음 —
   채널별 대상 건수만 적어두고, 키 확보 후 매칭 진행되면 이 문서를 채우겠다고 명시.
2. **평점 매칭 미매칭 28곳**: 3번 작업에서 나온 재시도 로그(`_retry-still-null.json`,
   내용 정리 후 삭제함)를 아래 3가지로 자동 분류해서 표로 정리:
   - 평점데이터 없음 추정(이름은 정확히 일치) — 11곳
   - 동명/유사상호 다수(특정 불가) — 8곳
   - 검색 자체 안됨(영문 전용 등록 상호 포함) — 9곳
   - "마피아떡볶이 본점" 주소의 `인천 서해구`(존재하지 않는 행정구역)도 오탈자로 보여 별도 표기함

명백히 식당이 아닌 항목은 이번 라운드에서 발견되지 않음 (별도 목록 없음).

**→ 1~4번 작업 전부 완료 (1번은 API 키 대기 상태로 보류).**

---

## 2026-08-16 (Day 2) — YouTube API 키 확보, 1번 작업 재개

사용자가 `data/.env.txt`에 `YOUTUBE_API_KEY` 추가함. 키 검증(`search.list` 테스트) 통과.

### 1-2. 실채널 4곳 영상 매칭 — ✅ 완료

**채널 ID 확인:**

| 채널 | 채널 ID | 실제 유튜브 채널명 |
|---|---|---|
| 성시경의 먹을텐데 | UCl23-Cci_SMqyGXE1T_LYUg | 성시경 SUNG SI KYUNG (본채널 내 코너) |
| 또간집(풍자) | UC4ZA57iJrf73bJlApKFeLRw | 스튜디오 수제 |
| 승우아빠 | UCgsffS7MfKL6YU3r_U3E-aA | 승우아빠 |
| 쯔양 | UCfpaSruWW3S4dibonKXENjA | tzuyang쯔양 |

`data/fetch-channel-videos.js`로 `playlistItems` API(quota 1 unit/call)를 이용해
채널별 전체 업로드 영상(제목+설명)을 로컬 캐시(`_yt-cache/`, gitignore 처리)에 저장.
성시경 1100개 / 또간집 1365개 / 승우아빠 1410개 / 쯔양 1115개.

**매칭 방식** (`data/match-youtube-videos.js`): 식당명을 정규화해 영상 제목+설명 텍스트에서
검색. **정확히 1건**만 걸리면 확정 매칭, **0건**이면 "검색안됨", **2건 이상**이면
"애매함"으로 스킵(억지로 고르지 않음). videoTitle은 실제 영상 제목 그대로 사용,
recommendNote는 원문 복사 없이 기존 것 유지(스펙상 다듬기는 선택사항이라 확실한 경우만 손댐).

**⚠️ 오탐 발견 및 수정**: 첫 승우아빠 실행에서 "오프닝"(실제 식당명)이
"통연어 무한으로 즐기기"라는 무관한 영상에 매칭됨 — 원인은 그 영상 설명란의
타임스탬프 챕터 라벨("오프닝"=인트로라는 뜻)과 우연히 문자열이 일치한 것.
즉시 되돌리고, 스크립트에 일반 단어 블랙리스트(오프닝/인트로/하이라이트/구독 등)를
추가해 이런 보일러플레이트 단어 단독으로는 매칭하지 않도록 안전장치를 넣음.

**채널별 결과:**

| 채널 | 매칭 | 검색안됨 | 애매(스킵) | 대상 |
|---|---|---|---|---|
| 성시경의 먹을텐데 | **29** | 4 | 5 | 38 |
| 쯔양 | **74** | 359 | 3 | 436 |
| 또간집(풍자) | 0 | 28 | 0 | 28 |
| 승우아빠 | 0 | 62 | 2 | 64 |

**또간집(풍자)/승우아빠가 0건인 이유**: 두 채널 모두 영상 제목이 펀치라인 위주("업보는
돌아오는거야", "말복엔 무조건")이고 설명란도 협업문의 등 보일러플레이트뿐, 식당명이
텍스트로 전혀 노출되지 않음(부분 검색으로도 0건 확인). 반면 성시경/쯔양은 제목이
"성시경의 먹을텐데 l 지역 식당명" 형식이거나 설명란에 `[식당명] 주소` 형태로 실제
상호가 명시돼 있어 매칭이 잘 됨. 이건 텍스트 메타데이터만으로는 원천적인 한계이고,
영상 자막/음성 인식까지 가야 커버 가능한데 이번 작업 범위(제목+설명) 밖이라 진행하지 않음.

**흥미로운 발견**: 쯔양 매칭 중 서로 다른 식당명 여러 개가 같은 영상 하나에 매칭되는
경우가 있었는데(예: 뜨란채/녹원/대궁/금은옥 → 전부 "순창 삼합" 영상), 확인해보니
지자체 협찬으로 한 영상에서 여러 맛집을 함께 소개하는 "맛집 투어" 영상이라 실제로
맞는 매칭이었음(설명란에 각 식당 주소가 전부 나열돼 있음).

**→ 전체 660곳 중 videoUrl 채워짐: 103건 (쯔양 74 + 성시경 29), 632→632 rating은 불변**

### 1-3. TV 방송 채널 4곳 매칭 시도 — ✅ 완료 (5건 추가 매칭)

방송사 공식 유튜브 채널 조사 결과, 예상대로 프로그램 전용 채널이 아니라
방송사 종합 채널에 클립이 섞여 올라가는 경우가 많았습니다 (사용자가 미리 우려했던
"여러 채널에 흩어져 있어서 매칭이 어렵다"는 상황과 일치). 채널별로 하나씩 골라 시도:

| 프로그램 | 실제 사용한 채널 | 비고 |
|---|---|---|
| 최자로드 | CHOIZA ROAD - 최자로드 (330개 영상) | 프로그램 전용 채널, 필터 불필요 |
| 식객 허영만의 백반기행 | TVCHOSUN LIFE (5,007개 중 `#백반기행` 태그 277개) | TV조선 종합 채널이라 제목 태그로 필터링 |
| 전지적 참견 시점(이영자) | 엠뚜루마뚜루: MBC 공식 종합 채널 (14,898개 중 `전참시`/`이영자` 태그 1,664개) | MBC 종합 채널이라 태그로 필터링 |
| 맛있는 녀석들 | 맛있는 녀석들 (Tasty Guys) (5,964개) | 프로그램 전용 채널 |

**결과:**

| 채널 | 매칭 | 대상 |
|---|---|---|
| 최자로드 | **5** | 40 |
| 식객 허영만의 백반기행 | 0 | 44 |
| 전지적 참견 시점(이영자) | 0 | 26 |
| 맛있는 녀석들 | 0 | 2 |

백반기행/이영자 0건인 이유: 방송 클립 편집 제목·설명이 "출연자 리액션/케미" 중심이라
(예: "리센느 원이X메이에게 LOVE ATTACK", "전현무 또 눈 뒤집혔네") 식당명이 텍스트에
거의 노출되지 않음. 이건 사용자가 사전에 예상한 대로라 무리해서 더 파고들지 않음.

**⚠️ 오탐 2번째 발견 및 근본 수정**: 이영자 채널 1차 실행에서 "서보"(실제 식당명, 2글자)가
"저~~~멀리서 보면 별로 차이 안 나는 박재범과 박쥐범" 이라는 완전 무관한 영상에 매칭됨 —
원인은 "멀리**서** **보**면"에서 공백을 사이에 둔 서로 다른 두 단어의 경계를 걸쳐
"서보"가 우연히 만들어진 것. "오프닝" 오탐(보일러플레이트 단어)과는 다른 종류의 버그.

즉시 되돌리고, 매칭 스크립트를 근본적으로 수정: 이름이 3자 이하로 짧을 때는
텍스트의 공백을 제거하지 않고 그대로 유지한 채 비교하도록 바꿈(공백이 있으면
그 자리에서 문자열이 끊기므로 서로 다른 단어를 걸치는 우연일치가 원천 차단됨).
긴 이름(4자 이상)은 기존처럼 공백 제거 비교도 허용(지점명 표기 차이 흡수 목적).
기존에 매칭된 2~3자 항목(녹원/대궁/몽탄/송옥/일광/청수/초원/목로)은 전부 재검증해서
전부 "짧은 이름이 단어 하나 안에 온전히 들어있는" 정상 케이스임을 확인함 — 되돌린 건
오프닝, 서보 딱 2건뿐.

**→ 전체 660곳 중 videoUrl 채워짐 최종: 108건 (쯔양 74 + 성시경 29 + 최자로드 5)**

**1번 작업(유튜브 영상 매칭) 전체 완료.** 실채널 4곳 우선 처리 → TV 채널 4곳 가능한 만큼
시도, 총 8개 채널 모두 시도 완료. 텍스트 메타데이터(제목+설명)만으로는 원천적 한계가
있는 채널(또간집/승우아빠/백반기행/이영자/맛있는 녀석들)은 정직하게 낮은 매칭률로 기록.

---
